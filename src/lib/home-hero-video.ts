/** Homepage hero background video (served from `/public/videos`). Bump `v` when replacing the file. */
export const HOME_HERO_VIDEO_SRC = "/videos/hcb-hero-video.mp4?v=2";

/** Static poster for hero LCP on mobile and video `poster` on desktop (optimized via `next/image`). */
export const HOME_HERO_POSTER_SRC =
  "/images/hcb/outdoor-multi-court-pickleball-basketball-tennis-boise-id-hatz-court-builders.jpg";
